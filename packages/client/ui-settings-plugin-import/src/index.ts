/** Host loader entry for the import-tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the plugin import tab. */
export function apply(): void {}
