/** Optional profile bundle marker for the governance plugin. */
export const name = 'agent-governance-bundle'

/** This bundle contributes its runtime row through cordis.patch.yml. */
export function apply(): void {}
