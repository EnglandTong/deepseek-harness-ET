/**
 * `@deepseek-ai/dsh-personal-supervisor` is a configuration bundle. Its
 * installable behavior is declared by `cordis.patch.yml`; this module keeps
 * the package compatible with the standard bundle build without adding a
 * second runtime entry point.
 * @module @deepseek-ai/dsh-personal-supervisor
 */

export {}
